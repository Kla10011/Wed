<!DOCTYPE html>
<html>
<head>
    <title>Data from MongoDB</title>
</head>
<body>
    <h1>Data from MongoDBggg</h1>
    <ul>
        <% data.forEach(item => { %>
            <li><%= item.name %>: <%= item.value %></li>
        <% }) %>
    </ul>
</body>
</html>